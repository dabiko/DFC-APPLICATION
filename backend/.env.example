# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_SETTINGS_MODULE=config.settings.development
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (Using Local PostgreSQL)
# Connecting to PostgreSQL running locally
DATABASE_URL=postgres://postgres:dabiko@localhost:5432/dfc_database
DB_ENGINE=django.db.backends.postgresql
DB_NAME=dfc_database
DB_USER=postgres
DB_PASSWORD=dabiko
DB_HOST=127.0.0.1
DB_PORT=5432

# MinIO Storage Configuration (Using Docker MinIO)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=dfc_minio_admin
MINIO_SECRET_KEY=dfc_minio_password_2025
MINIO_BUCKET_NAME=dfc-documents
MINIO_USE_SSL=False

# Elasticsearch Configuration
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_USE_SSL=False

# Redis Configuration
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Celery Configuration (Using Docker RabbitMQ and Redis)
CELERY_BROKER_URL=amqp://dfc_rabbit:dfc_rabbit_password_2025@localhost:5672//
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# Security Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60 # minutes
JWT_REFRESH_TOKEN_LIFETIME=1440  # minutes (24 hours)

# Email Configuration (Resend)
# For development: Use console backend (emails printed to console)
# For production: Use Resend API (get your API key from https://resend.com)
#EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
#RESEND_API_KEY=re_FUoXTTqy_6vi69y682nAf5PLw8Azoq5HP
#USE_RESEND_IN_DEV=False
#DEFAULT_FROM_EMAIL=DFC <onboarding@resend.dev>
#SUPPORT_EMAIL=support@yourdomain.com
#FRONTEND_URL=http://localhost:3000

 # Email Configuration - Gmail SMTP
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=btamunang@quantum-soft.ai
EMAIL_HOST_PASSWORD=ekjwfidjivrlvpau
DEFAULT_FROM_EMAIL=DFC <btamunang@quantum-soft.ai>
SUPPORT_EMAIL=btamunang@quantum-soft.ai
FRONTEND_URL=http://localhost:3000
USE_RESEND_IN_DEV=True

# Optional: Local SMTP for testing (if you want to test with MailHog/MailDev)
EMAIL_HOST=localhost
EMAIL_PORT=1025

# Sentry (Error Tracking)
SENTRY_DSN=

# Environment
ENVIRONMENT=development
